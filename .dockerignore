.env
.git.git
.venv
.vscode
__pycache__
*.env
*.log
.docker-cache/
.docker-cache-new/
uploads/
--- a/d:\Bots\Vera\README.md
+++ b/d:\Bots\Vera\README.md
@@ -31,26 +31,22 @@
    ```
 
 3. **Запустите приложение**
-   ```bash
-   # Сборка и запуск
-   ./build.sh
-   
-   # Или вручную
-   docker compose up -d
-   ```
+   Для сборки, тестирования и сканирования уязвимостей выполните:
+   ```bash
+   ./build.sh
+   ```
+   После успешной сборки, запустите сервисы в фоновом режиме:
+   ```bash
+   docker compose up -d
+   ```
 
-### Запуск
+### Остановка
+   ```bash
+   docker compose down
+   ```
 
-```bash
-# Запуск основного приложения
-uvicorn app.main:app --reload
-
-# Или с помощью Docker
-docker-compose up --build
-```
-
 ## 🧪 Запуск тестов
 
 ```bash
